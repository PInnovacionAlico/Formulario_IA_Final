# ======================================
# EXAMPLE .env FILE FOR LOCAL DEVELOPMENT
# ======================================
# Copy this file to .env and fill in real values
# DO NOT commit real secrets to git!

# ======================================
# DATABASE & STORAGE
# ======================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ======================================
# SERVER CONFIGURATION
# ======================================
PORT=3000
NODE_ENV=development

# ======================================
# SECURITY & AUTHENTICATION
# ======================================
# IMPORTANT: Change this to a strong random string in production (min 32 chars)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here

# CORS Configuration
# Comma-separated list of allowed origins. Use * for all origins (not recommended for production)
# Production example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=*

# ======================================
# WEBHOOKS
# ======================================
# Set to true to disable webhooks during local development
DISABLE_WEBHOOK=true

# Webhook API Key para autenticación (se envía en header: x-api-key)
WEBHOOK_API_KEY=your-webhook-api-key-here

# Webhook para registro de usuarios
WEBHOOK_REGISTRO_USUARIO=https://your-webhook-url.com/registro-usuario

# Webhook para reset de contraseña
PASSWORD_RESET_WEBHOOK_URL=https://your-webhook-url.com/reset-password

# Webhook para envío de formularios IA
WEBHOOK_AI_FORM=https://your-webhook-url.com/ai-form

# ======================================
# EMAIL CONFIGURATION (Optional - if using custom email service)
# ======================================
# EMAIL_SERVICE=smtp
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=noreply@example.com
# EMAIL_PASSWORD=your-email-password
# EMAIL_FROM=noreply@example.com

# ======================================
# RATE LIMITING (Optional - defaults are set in code)
# ======================================
# Auth endpoints: max requests per window
# RATE_LIMIT_AUTH_MAX=5
# RATE_LIMIT_AUTH_WINDOW_MS=900000

# General API: max requests per window
# RATE_LIMIT_GENERAL_MAX=100

# Upload endpoint: max uploads per window
# RATE_LIMIT_UPLOAD_MAX=20

# ======================================
# FILE UPLOAD LIMITS (Optional - defaults in code)
# ======================================
# Maximum file size in bytes (default: 5MB)
# MAX_FILE_SIZE=5242880

# Maximum photos per user (default: 4)
# MAX_USER_PHOTOS=4

# ======================================
# EXTERNAL SERVICES (Optional)
# ======================================
# CDN_URL=https://cdn.yourdomain.com
# FRONTEND_URL=https://yourdomain.com

# Optional environment indicator
NODE_ENV=development

# Notes:
# - For basic UI serving you don't need Supabase keys; the server will still start but API routes that call Supabase will fail.
# - Keep secrets out of source control. Use a local .env or your OS secret manager.
