# Example .env file for local development
# Copy to .env and fill real values. DO NOT commit real secrets to git.

# Server port (optional)
PORT=3000

# JWT secret used to sign/verify tokens (required for auth endpoints)
# IMPORTANT: Change this to a strong random string in production!
JWT_SECRET=your_jwt_secret_here

# Supabase service role key and URL (required for DB/storage operations)
# Create a Supabase project and put your project's values here.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Webhook settings (optional). If you don't want outgoing webhooks during local dev, set DISABLE_WEBHOOK=true
WEBHOOK_URL=https://your-webhook-url.example/receive
# Specific webhook for password reset emails (optional - falls back to WEBHOOK_URL if not set)
PASSWORD_RESET_WEBHOOK_URL=https://apps.alico-sa.com/webhook/reset-password-ai-form
DISABLE_WEBHOOK=true

# CORS Configuration (optional)
# Comma-separated list of allowed origins. Use * for all origins (not recommended for production)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=*

# Optional environment indicator
NODE_ENV=development

# Notes:
# - For basic UI serving you don't need Supabase keys; the server will still start but API routes that call Supabase will fail.
# - Keep secrets out of source control. Use a local .env or your OS secret manager.
